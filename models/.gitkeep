# This file ensures the models directory is tracked by Git
# Large model files (*.safetensors, *.bin) are excluded due to GitHub's 100MB file size limit
# Models will be automatically downloaded by setup_models.py when the application starts
